<section id="tombola" class="py-20 bg-gray-100">
    <div class="container mx-auto px-6">
        <div class="text-center mb-16">
                <span class="inline-block px-3 py-1 bg-blue-100 text-blue-800 rounded-full text-sm font-semibold tracking-wider mb-4">
                    TOMBOLA
                </span>
            <h2 class="text-3xl md:text-4xl font-bold section-title section-title-font">GRAND TIRAGE AU SORT</h2>
            <p class="text-gray-600 mt-4 max-w-4xl mx-auto">
                Tentez votre chance en prenant vos billets pour la <span class="font-bold text-blue-600">Tombola de l'Aérofestival 2025 </span>!<br>
                Au total, c'est plus de <span class="font-bold">2000€ de lots exceptionnels</span> à gagner tels que des équipements, des bons d’achat, des expériences uniques et bien plus encore !.<br>
            </p>
        </div>

        <div class="max-w-4xl mx-auto">
            <div class="bg-blue-50 rounded-xl p-8 md:p-12 shadow-lg mb-12">
                <h3 class="text-2xl font-bold mb-6 text-gray-800 section-title-font">PARTICIPEZ À LA TOMBOLA</h3>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                    <div class="bg-white p-6 rounded-lg shadow">
                        <div class="text-blue-600 text-3xl mb-3">
                            <i class="fas fa-ticket-alt"></i>
                        </div>
                        <h4 class="font-bold text-lg mb-2">Tirage au sort</h4>
                        <p class="text-gray-600">Le tirage aura lieu le <span class="font-bold text-blue-600">dimanche 06 juillet</span> à <strong>17h</strong> sur la scène principale en présence de l'équipe organisatrice.</p>
                    </div>
                    <div class="bg-white p-6 rounded-lg shadow">
                        <div class="text-blue-600 text-3xl mb-3">
                            <i class="fas fa-info-circle"></i>
                        </div>
                        <h4 class="font-bold text-lg mb-2">Participation</h4>
                        <p class="text-gray-600">Les billets sont en vente <strong>dès maintenant à l’aéroclub</strong> et durant toute la semaine de l’Aérofestival.</p>
                    </div>
                </div>
            </div>
        </div>

        <h3 class="text-2xl font-bold mb-8 text-center text-gray-800 section-title-font">LOTS À GAGNER</h3>

        <div class="relative max-w-7xl mx-auto">
            {% if prizes is empty %}
                <p class="flex justify-center">La liste des lots arrive prochainement...</p>
            {% endif %}
            <div class="swiper tombola-swiper">
                <div class="swiper-wrapper">
                    {% for prize in prizes %}
                        {{ include('components/tombola_card.html.twig') }}
                    {% endfor %}
                </div>

                <div class="swiper-button-prev"></div>
                <div class="swiper-button-next"></div>
            </div>
        </div>
    </div>
</section>