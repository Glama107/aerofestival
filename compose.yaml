services:
  database:
    image: postgres:16
    environment:
      POSTGRES_DB: app_aerofestival
      POSTGRES_USER: app_user
      POSTGRES_PASSWORD: app_password
    ports:
      - '5432:5432'
    volumes:
      - ./postgres:/var/lib/postgresql/data
